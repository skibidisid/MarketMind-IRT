LSTM_model.py - > creates and trains the model

BestLSTM.py - > utilizes the saved, trained model to create predictions

best_lstm_model.h5 - > the saved LSTM (created in LSTM_model.py)

scaler.save - > The scale of the test data (saved in LSTM_model.py), 
so the prediction will utilize the same scale of the data and predicts correctly
